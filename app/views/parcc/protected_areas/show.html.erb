<section class="back-link">
  <%= render "back_link" %>
</section>

<section>
  <h1> Climate change vulnerability assessments </h1>
  <div class="line"></div>

  <%= render "protected_area_info" %>
</section>

<section class="tva-sdm">
  <%= render "tva_percentage" %>
  <%= render "sdm_percentage" %>
</section>

<section class="vulnerable-species">
  <h2>Vulnerable <%= taxonomic_classes_dropdown "vulnerability"%> species</h2><br>
  <%= render "vulnerable_species_table" %>
</section>

<section class="suitability">
  <h2>Expected changes in climate suitability for <%= taxonomic_classes_dropdown "suitability" %></h2><br>
  <%= render "suitability_table" %>
</section>

<section class="download">
  <%= render "download_data" %>
</section>

<section class="back-link">
  <%= render "back_link" %>
</section>
