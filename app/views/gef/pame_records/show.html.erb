  <h1> <%= @assessment[:name] %>
      WDPA ID #<%= @assessment[:wdpa_id] %> - Assessment <%= @assessment[:mett_original_uid] %></h1>

  <div class="gef-main-container">
    <table class="table">
      <% @assessment.each do |parameter,value| %>
          <tr>
            <td colspan="3"><%=t parameter %></td>
            <td class="text-center"><%= value %></td>
          </tr>
      <% end %>
    </table>
  </div>
</main>
